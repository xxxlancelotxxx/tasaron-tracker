# Database (PostgreSQL via Docker - already configured in docker-compose.yml)
# No changes needed for local development!
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/tasaron_tracker

# For production deployment only:
# DATABASE_URL=postgresql://user:password@host:5432/database